{
  "name": "shadowbox-backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "ShadowBox: detonates suspicious URLs in a sandboxed headless browser and returns a risk verdict.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test:local": "node test/detonate-local.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "ipaddr.js": "^2.4.0",
    "puppeteer": "^24.10.0"
  }
}
